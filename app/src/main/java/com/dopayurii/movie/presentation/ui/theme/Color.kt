package com.dopayurii.movie.presentation.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Blue color variant for dark theme (80% lightness).
 * Used as primary color in dark color scheme.
 */
val Blue80 = Color(0xFF90CAF9)

/**
 * Blue grey color variant for dark theme (80% lightness).
 * Used as secondary color in dark color scheme.
 */
val BlueGrey80 = Color(0xFFB0C4DE)

/**
 * Pink color variant for dark theme (80% lightness).
 * Used as tertiary color in dark color scheme.
 */
val Pink80 = Color(0xFFEFB8C8)

/**
 * Blue color variant for light theme (40% lightness).
 * Used as primary color in light color scheme.
 */
val Blue40 = Color(0xFF1976D2)

/**
 * Blue grey color variant for light theme (40% lightness).
 * Used as secondary color in light color scheme.
 */
val BlueGrey40 = Color(0xFF455A64)

/**
 * Pink color variant for light theme (40% lightness).
 * Used as tertiary color in light color scheme.
 */
val Pink40 = Color(0xFF7D5260)
